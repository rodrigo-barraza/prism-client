export const metadata = {
  title: "Prompts",
  description:
    "Create, store, and manage your own reusable prompts and messages for AI conversations.",
};

export default function PromptsLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

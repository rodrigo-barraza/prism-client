export const metadata = {
  title: "Skills",
  description:
    "Per-skill usage over the last 30 days: invocations, last use, and what each skill's catalog line and body cost in tokens.",
};

export default function AdminSkillsLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

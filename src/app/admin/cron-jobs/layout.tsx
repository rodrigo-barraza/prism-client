export const metadata = {
  title: "Cron Jobs — Admin",
  description:
    "View and manage all scheduled cron jobs across every project and user.",
};

export default function AdminCronJobsLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

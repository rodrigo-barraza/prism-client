export const metadata = {
  title: "Chat",
  description:
    "Unified admin viewer for all AI conversations. Filter by agent, view messages, settings, and conversation stats in real time.",
};

export default function AdminChatLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

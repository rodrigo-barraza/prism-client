export const metadata = {
  title: "Sessions — Prism Admin",
  description: "Interactive node graph visualization of agent sessions, models, tools, and requests",
};

export default function SessionsLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

export const metadata = {
  title: "Scheduled Tasks — Admin",
  description:
    "View and manage all scheduled tasks across every project and user.",
};

export default function AdminScheduledTasksLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

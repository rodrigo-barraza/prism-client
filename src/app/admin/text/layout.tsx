export const metadata = {
  title: "Text",
  description:
    "Manage text-to-speech and speech-to-text assets and history from the admin dashboard.",
};

// @ts-ignore
export default function AdminTextLayout({ children: any }) {
  // @ts-ignore
  return children;
}

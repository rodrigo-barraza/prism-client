export const metadata = {
  title: "Users",
  description:
    "View all users and their usage statistics within the Prism AI Gateway.",
};

export default function AdminUsersLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}

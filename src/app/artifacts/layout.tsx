export const metadata = {
  title: "Artifacts",
  description:
    "Browse everything the agent has made — documents, images, embeds, video, and audio.",
};

export default function ArtifactsLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
